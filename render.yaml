services:
  - type: web
    name: insightport
    runtime: docker
    plan: free
    healthCheckPath: /healthz
    envVars:
      - key: FB_USER_TOKEN
        sync: false   # prompted in the Render dashboard, never stored in git
      - key: APP_PASSWORD
        sync: false   # basic-auth password for the app (any username)
